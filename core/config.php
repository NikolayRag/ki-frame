<?
/*
Initial configuration script for setting environment.
Should be runned once at deploy.
*/
// -todo 93 (config) +0: create initial config script

// -todo 94 (config) +0: create /private folder structure, c_core.php config with DB fields filled, and /web/index.php with basic setup
?>